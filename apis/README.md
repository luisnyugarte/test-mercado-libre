# Express js serve with node

## Para utilizar las dependencias del servidor, es necesario ejecutar el comando:
```
npm install
```
## Luego inicializar el servidor utilizando el comando:

```
node index.js
```